version: 2.1
jobs:
  build:
    docker:
      - image: cimg/base:stable
    steps:
      - checkout
      - run: echo "CircleCI 동작 확인"
workflows:
  version: 2
  build-and-test:
    jobs:
      - build
